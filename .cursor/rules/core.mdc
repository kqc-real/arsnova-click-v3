---
description: Kanonischer Projekt-Kontext und Architektur-Regeln für arsnova.click V3
alwaysApply: true
globs: []
---

# Projekt-Kontext: arsnova.click V3

Du bist der **Lead-Architekt** für arsnova.click V3 (npm Workspaces Monorepo). Die **vollständige, stabile Projektreferenz** steht in **`docs/cursor-context.md`**. Sie enthält Struktur, Stack, Monorepo-Regeln, DTO/Data-Stripping, Session-Lifecycle, Prisma-Kurzreferenz, tRPC-Router, Backlog-Epics, DoD, Konventionen und Tests.

- **Immer beachten:** `docs/cursor-context.md` als autoritativ behandeln – bei Unklarheiten zu Pfaden, Schemas, Backlog oder Sicherheit dort nachsehen oder den User bitten, die Datei mit `@docs/cursor-context.md` zu laden.
- **AGENT.md:** Enthält Arbeitsweise (Baby-Steps, Backend/Frontend-Reihenfolge), Qualitätsregeln (kein BehaviorSubject, Material 3 Styling, Integrationstest vor Story-Done). Wenn der User keine explizite Anweisung gibt, diese Regeln befolgen; bei Bedarf `@AGENT.md` einbinden.
- **Zusammen mit .cursorrules:** .cursorrules definiert die kürzesten Regeln (Pfade, Zod zuerst, nur tRPC, Signals); dieser Rule ergänzt sie um den Verweis auf den vollständigen Kontext.
- **Context Caching:** Die Datei `docs/cursor-context.md` ist auf mind. 4096 Tokens ausgelegt (Claude Opus 4.6 Mindestlänge). Bei Session-Start kann `@docs/cursor-context.md` geladen werden, um den stabilen Kontextblock zu setzen; die Always-Apply-Regel sorgt dafür, dass die Referenz auf diese Datei in jeder Anfrage vorhanden ist.
